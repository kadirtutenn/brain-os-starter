# The runtime store is machine-local and disposable — never commit it.
.brain-runtime/
# Python bytecode
__pycache__/
*.pyc
# MCP server virtualenv created by mcp/install.sh
.venv/
# OS / editor cruft
.DS_Store
.obsidian/
.trash/
# settings backups created by install.sh
*.bak-brain-install
