# Lessons INDEX

Single-line map. Open the relevant file for detail. The hook injects a file
based on the keywords in these lines. Keep the line format intact:
`- [[target]] — kw1, kw2, kw3` (the hook parses it).

Placeholder rows (no `— <keywords>` tail, so the hook skips them on a fresh
vault). Add the tail — `- [[target]] — kw1, kw2, kw3` — once you record a lesson.

## process
- [[process/problems]]
- [[process/patterns]]

## data
- [[data/patterns]]
