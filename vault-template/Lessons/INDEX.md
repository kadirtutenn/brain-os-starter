# Lessons INDEX

Single-line map. Open the relevant file for detail. The hook injects a file
based on the keywords in these lines. Keep the line format intact:
`- [[target]] — kw1, kw2, kw3` (the hook parses it).

## process
- [[process/problems]] — (add keywords as you record bug/process lessons)
- [[process/patterns]] — (add keywords as you record reusable process patterns)

## data
- [[data/patterns]] — (add keywords as you record data/query patterns)
